export interface User {
  name: string;
  avatar: string;
}

export interface Config {
  name: string;
  logo: string;
}

export interface Route {
  name: string;
  path: string;
  isActive: boolean;
}